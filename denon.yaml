scripts:
    start:
        cmd: "deno run ./src/main.ts"
        desc: "Debug application"
        unstable: true
        watch: false
        allow:
            - env
            - read
            - plugin
    build:
        cmd: "deno run -A scripts/build.ts"
        desc: "Build application"
        unstable: true
        watch: false
